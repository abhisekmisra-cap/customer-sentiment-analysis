# Customer Comment Analyzer - MCP-Enabled Application with Azure OpenAI
# =======================================================================
# Python 3.8+ required

# LangChain and Azure OpenAI Integration
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.1
langchain-core>=0.1.0

# Optional: Keep HuggingFace for local models if needed
# langchain-huggingface>=0.0.1
# transformers>=4.35.0
# torch>=2.0.0
# huggingface-hub>=0.19.0
# sentencepiece>=0.1.99
# accelerate>=0.25.0

# Web Application (for app.py)
streamlit>=1.28.0
pandas>=2.0.0

# Model Context Protocol (for MCP architecture)
mcp>=0.9.0

# Note: The following are built-in Python modules (no installation needed):
# - asyncio (Python 3.7+)
# - json (Python standard library)
# - os (Python standard library)
# - pathlib (Python 3.4+)
# - sys (Python standard library)
# - typing (Python 3.5+)
# - datetime (Python standard library)
# - subprocess (Python standard library)
# - threading (Python standard library)
# - queue (Python standard library)
# - time (Python standard library)
# - traceback (Python standard library)
